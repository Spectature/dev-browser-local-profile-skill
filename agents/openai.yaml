interface:
  display_name: "Dev Browser Local Profile"
  short_description: "Connect dev-browser to a persistent local Chrome profile."
  default_prompt: "Use $dev-browser-local-profile to open a local site in an existing logged-in browser profile."

